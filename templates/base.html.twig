<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>
            {% block title %}Welcome!
            {% endblock %}
        </title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    </head>
    <body>
        <header class="">
            <nav class="navbar has-background-link-dark" role="navigation" aria-label="main navigation">
                <div class="navbar-brand ">
                    <a class="navbar-item has-text-white" href="">
                        Central-oCar
                    </a>

                    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
                        <span aria-hidden="true"></span>
                        <span aria-hidden="true"></span>
                        <span aria-hidden="true"></span>
                    </a>
                </div>

                <div id="navbarBasicExample" class="navbar-menu">
                    <div class="navbar-start">
                        <a class="navbar-item has-text-white">
                            Les annonces
                        </a>

                        <a class="navbar-item has-text-white">
                            Déposer mon annonce
                        </a>
                    </div>

                    <div class="navbar-end">
                        <div class="navbar-item">
                        {% if app.user %}
                            <p class="has-text-white mr-3">Bonjour {{app.user.email}}</p>
                            <a href="{{path('app_logout')}}" class="button has-background-link-dark has-text-light">
                                <strong>Deconnexion</strong>
                            </a>
                        {% else %}
                            <div class="buttons">
                                <a href="{{path('app_login')}}" class="button has-background-link-dark has-text-light">
                                    <strong>Connexion</strong>
                                </a>
                            </div>
                        {% endif %}
                        </div>
                    </div>
                </div>
            </nav>
        </header>
        {% block body %}{% endblock %}
    </body>
</html>